<div class="contents row">
  <% @posts.each do |post| %>
    <div class="content_user" style="background-image: url(<%= user.image %>);">
      <p><%= post.text %></p>
      <span class="name">
        <%= user.name %>
      </span>
    </div>
  <% end %>
</div>
